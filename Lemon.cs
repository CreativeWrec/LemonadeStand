namespace LemonadeStand_3DayStarter
{
    class Lemon 
    {
        // member variables (HAS A)
        // constructor (SPAWNER)
        public Lemon()
        {
           
        }

        // member methods (CAN DO)
    }
}
